const NotFound = () => {
    return (
        <div className="text-indigo-400 text-center pt-100 text-8xl font-bold z-50 bg-white h-screen w-full fixed">
            404 — nicht gefunden.
        </div>
    );
}

export default NotFound;
